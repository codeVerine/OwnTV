case "$(uname -s)" in
  Darwin) export JAVA_HOME="${JAVA_HOME:-$(/usr/libexec/java_home 2>/dev/null || printf '%s\n' '/Applications/Android Studio.app/Contents/jbr/Contents/Home')}" ;;
  Linux) export JAVA_HOME="${JAVA_HOME:-$(dirname "$(dirname "$(readlink -f "$(command -v java)")")")}" ;;
esac
